# Naxshatra AI — Website

This repository contains the Naxshatra AI company website built with Vite, React, TypeScript, and Tailwind CSS.

## Quick start

1. Install dependencies

```powershell
cd "c:\\Users\\Atharva\\OneDrive\\Desktop\\naxshatra-ai-engage-main"
npm install
```

2. Run dev server

```powershell
npm run dev
```

3. Build for production

```powershell
npm run build
npm run preview
```

## Project structure

- `src/` — source code (components, pages, assets)
- `public/` — static files (favicon, robots.txt)
- `dist/` — production build output

## Notes

- The project no longer includes external references to "Lovable" or `lovable.dev`.
- To update installed packages after changes to `package.json`, run `npm install`.

## Deploy

You can deploy the `dist/` output to Vercel, Netlify, GitHub Pages, or any static host.

---

If you want, I can run `npm install` to update `package-lock.json` and make a commit with these changes. Would you like me to do that next?
